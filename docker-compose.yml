services:

  hrdata:

    build: .

    container_name: hrdata

    ports:
      - "8501:8501"

    volumes:
      - .:/app

    working_dir: /app

    command: >
      streamlit run app_hr.py
      --server.address=0.0.0.0
      --server.port=8501

    restart: unless-stopped